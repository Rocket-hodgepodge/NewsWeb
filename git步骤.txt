1 先将本地文件存入缓存
git add .
git commit -m '理由'

2 推送到git 
git push origin ren(分支名)
输入git用户 密码

3 切换到主分支
git checkout master

4 把主分支更新的代码拉下来
git pull orgin master

5 切换回自己的分支
git checkout ren

6 查询状态
git merge master
